const ConnectToMap = () => {
  return (
    <button className="text-sm bg-transparent text-black hover:text-gray-500 hover:border-transparent focus:outline-none">
      내 위치로 찾기
    </button>
  );
};

export default ConnectToMap;
